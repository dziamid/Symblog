{% extends 'BloggerBlogBundle::layout.html.twig' %}

{% block title %}
  Contact us
{% endblock %}

{% block body %}
    <header>
        <h1>Contact us</h1>
    </header>
    <article>
        <form action="{{ path('BBBundle_contact') }}" method="post" {{ form_enctype(form) }}>
            {{ form_widget(form) }}
            
            <input type="submit">
        </form>
    </article>
{% endblock %}
